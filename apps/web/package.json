{
  "name": "escbase-next-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "validate": "node scripts/validate-content.js",
    "build": "node scripts/validate-content.js && node scripts/generate-sitemap.js && node scripts/generate-feed.js && vite build"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
